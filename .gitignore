#Bỏ qua thư mục thư viện nặng

node_modules/

#Bỏ qua các file log lỗi của npm

npm-debug.log*
yarn-debug.log*
yarn-error.log*

#Bỏ qua file hệ thống của Mac và Windows (File rác)

.DS_Store
Thumbs.db

#Bỏ qua file cấu hình môi trường chứa mật khẩu (Bảo mật)

.env
.env.local
.env.development.local
.env.test.local
.env.production.local

#Bỏ qua thư mục build (nếu sau này có dùng framework build)

dist/
build/