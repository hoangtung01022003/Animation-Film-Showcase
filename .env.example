# ============================================
# BACKEND CONFIGURATION
# ============================================

# Database Configuration (PostgreSQL on Render)
# Lấy từ Render PostgreSQL Dashboard > Internal Database URL
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# JWT Authentication
# Tạo secret key mạnh: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production_minimum_32_characters

# JWT Token Expiration (ví dụ: 7d, 24h, 30m)
JWT_EXPIRES_IN=7d

# ============================================
# SERVER CONFIGURATION
# ============================================

# Port (Render tự động gán, nhưng local cần định nghĩa)
PORT=3000

# Node Environment (development / production / test)
NODE_ENV=development

# Server URL
# Development: http://localhost:3000
# Production: https://your-app-name.onrender.com
SERVER_URL=http://localhost:3000

# ============================================
# FRONTEND CONFIGURATION
# ============================================

# Frontend URL (Cho CORS - Chính là SERVER_URL trong fullstack)
CLIENT_URL=http://localhost:3000

# API Base URL (Frontend sẽ gọi API qua địa chỉ này)
# Development: http://localhost:3000/api
# Production: /api (relative path)
API_BASE_URL=/api

# ============================================
# DATABASE CONNECTION POOL
# ============================================

# Maximum number of clients in the pool
DB_POOL_MAX=20

# Idle timeout in milliseconds
DB_POOL_IDLE_TIMEOUT=30000

# Connection timeout in milliseconds
DB_POOL_CONNECTION_TIMEOUT=2000

# ============================================
# SECURITY SETTINGS
# ============================================

# Bcrypt Salt Rounds (10-12 recommended)
BCRYPT_SALT_ROUNDS=10

# Rate Limiting (requests per window)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# FILE UPLOAD (Nếu có tính năng upload avatar)
# ============================================

# Maximum file size in bytes (5MB)
MAX_FILE_SIZE=5242880

# Allowed file types
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp

# ============================================
# EMAIL CONFIGURATION (Tùy chọn - cho reset password)
# ============================================

# SMTP Settings
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ============================================
# LOGGING
# ============================================

# Log Level (error, warn, info, debug)
LOG_LEVEL=info

# ============================================
# RENDER-SPECIFIC SETTINGS
# ============================================

# Render tự động inject các biến này, không cần khai báo:
# - PORT (Render assigns automatically)
# - DATABASE_URL (From PostgreSQL service)
# - RENDER (boolean, true if running on Render)
# - RENDER_SERVICE_NAME
# - RENDER_INSTANCE_ID
# - RENDER_GIT_COMMIT

# ============================================
# NOTES FOR PRODUCTION (RENDER)
# ============================================
# 1. Không commit file .env lên Git
# 2. Cấu hình Environment Variables trên Render Dashboard
# 3. DATABASE_URL lấy từ Render PostgreSQL > Internal Database URL
# 4. NODE_ENV=production
# 5. JWT_SECRET phải là chuỗi ngẫu nhiên mạnh
# 6. CLIENT_URL = SERVER_URL = https://your-app-name.onrender.com